import 'package:flutter/material.dart';

class MOBasic extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.change_history, size: 128.0, color: Colors.black12);
  }
}